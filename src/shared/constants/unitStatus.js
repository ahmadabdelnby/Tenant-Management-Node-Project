/**
 * Unit Status Constants
 */
const UNIT_STATUS = {
  AVAILABLE: 'AVAILABLE',
  RENTED: 'RENTED',
};

const UNIT_STATUS_ARRAY = Object.values(UNIT_STATUS);

module.exports = {
  UNIT_STATUS,
  UNIT_STATUS_ARRAY,
};
